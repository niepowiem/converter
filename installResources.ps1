# Skrypt PowerShell do instalacji wymaganych bibliotek Pythona

# Instalacja PyYAML do obsługi plików .yml/.yaml
pip install pyyaml

# Instalacja biblioteki do obsługi plików .xml
pip install xmltodict

# Instalacja pyinstaller do budowania pliku .exe
pip install pyinstaller
